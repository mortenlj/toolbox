[tasks.docker-build]
description = "Build the docker image"
sources = ["files/**", "Dockerfile"]
run = "docker buildx build --tag ttl.sh/mortenlj-toolbox:1h ."

[task_config]
includes = [".config/mise-lib/virtual_tasks.toml", ".config/mise-lib"]
